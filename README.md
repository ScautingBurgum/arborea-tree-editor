This project uses [Yarn](https://yarnpkg.com/lang/en/).

To install and run in dev mode:

```shell
$ git clone https://github.com/raamwerk/arborea-tree-editor
$ cd arborea-tree-editor
$ yarn
$ yarn dev
```
